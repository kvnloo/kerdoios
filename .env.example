# Kerdoios does not read provider API keys from this file.
# Configure Hermes Bitwarden, then store GROQ_API_KEY, CEREBRAS_API_KEY,
# OPENROUTER_API_KEY (paid overflow), NVIDIA_API_KEY, XAI_API_KEY, and
# NOUS_API_KEY in that vault project.
#
#   hermes secrets bitwarden setup
#
# Public OpenRouter :free listing still works without a key.
# Doctor / enable fail closed until the vault is configured.
#
# Local endpoint URL is not a secret:
# KERDOIOS_LOCAL_BASE_URL=http://127.0.0.1:11434/v1
